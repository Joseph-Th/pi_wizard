# Status

## Phase

**The personal Windows desktop application is implemented through the deterministic Phase 5 boundary. Pi-native runtime/session orchestration, bounded parallel work, conservative Git-worktree lifecycle, paged/cancelable change review, renderer recovery, accessibility contracts, scale fixtures, migration/compatibility policy, and optimized Windows builds are implemented. Out-of-scope platform/distribution work is not a project gap.**

Research and the initial product/architecture baseline were completed on 2026-08-27.

The current implementation passes the repository `full` verification lane, including routine tests, strict lint/type checks, large-history/diff/concurrency fixtures, desktop startup measurement, release configuration checks, and an optimized Tauri desktop build.

A second research/audit pass on 2026-08-27 compared the live foundation against current Pi RPC/session/security documentation and recent Pi GUI, Codex, and OpenCode reliability reports. It tightened protocol, persistence, Stop/recovery, draft, attachment, worktree, renderer-recovery, and CSP contracts before runtime integration begins.

A third research/audit pass on 2026-08-27 focused on desktop environment parity, project/path identity, recoverable derived state, Pi trust/context-file semantics, cancellable session mutations, and bounded fire-and-forget extension UI state.

A fourth research/audit pass on 2026-08-27 rechecked Pi's current streaming RPC schema and current lightweight Pi/agent desktop implementations. It tightened content-indexed assistant streaming, direct RPC Bash correlation, aggregate hot-state bounds, and caught a misplaced runtime unit test that prevented an all-target core compile.

A fifth research/audit pass on 2026-08-27 focused on current Pi RPC concurrency, compaction/Stop behavior, direct Bash semantics, authoritative state recovery, desktop launch-environment parity, supervised subprocess ownership, and renderer backpressure. It moved several Phase 2 guarantees from design prose into Tauri-independent executable owners.

A sixth research/audit pass on 2026-08-27 added bounded shell/version probing, revisioned capabilities and extension UI projection, schema-versioned runtime hydration, and durable-cursor `get_entries(since)` synchronization. It also established the first Tauri hydration/probe IPC surface.

A seventh research/audit pass on 2026-08-27 joined the supervised child, RPC controller, RuntimeStore, Stop transaction, renderer backlog, and shutdown lifecycle under one persistent runtime manager. It also found and fixed a Windows wrapper-process defect where terminating an npm-style `.cmd` launcher could leave its Node descendant holding inherited pipes open indefinitely.

An eighth research/audit pass on 2026-08-27 tightened renderer recovery, extension interactions, session replacement, draft ownership, startup readiness, and retained runtime-state bounds. Ordinary hydration is now non-destructive, stale renderer backlog recovery is explicit per run, accepted Pi session replacements reconcile identity through `get_state`, extension `set_editor_text` enters a session-scoped backend draft owner, and a spawned child is not considered Ready until a bounded RPC handshake succeeds.

A ninth research/audit pass on 2026-08-27 focused on composer ownership and user-data durability. It added generation-captured Send/Steer/Follow-up transactions, atomic session-draft persistence and restart recovery, bounded shutdown flush, corruption quarantine, a restore-pending gate, and a persistent Solid composer whose rapid edits cannot create an unbounded IPC promise queue.

A tenth research/audit pass on 2026-08-27 rechecked Pi replacement/interrupt semantics and recent lightweight GUI session/project failures. It made session replacement wait for old-session draft durability, bounded `clear_queue` recovery before cloning it, restored Stop queue text ahead of existing unsent input without destructive overflow, made `ProjectId` durable through an atomic recoverable registry, added an end-to-end project launcher, and exposed the backend-bounded live assistant/tool/Bash projection in the renderer.

An eleventh research/audit pass on 2026-08-27 added bounded current-project session discovery/resume and a file-backed active-branch history reader. Cold history now pages through opaque session-bound cursors without issuing an unbounded initial `get_messages`/`get_entries`, and persisted user entries can drive Pi-native forks. The renderer keeps one bounded history page rather than accumulating a complete transcript.

A twelfth research/audit pass on 2026-08-27 rechecked current Pi model metadata, image routing, compaction, lightweight Pi GUIs, OpenCode attachment failures, and OpenChamber long-session/reconnect reports. Pi Wizard now retains only the narrow declared image-input capability needed by the UI, refuses to submit images when Pi explicitly marks the current model text-only, preserves unknown capability for compatibility, and exposes Pi's native manual compaction rather than implementing a second summarizer.

A thirteenth research/audit pass on 2026-08-27 rechecked current Pi session-stat/compaction semantics plus long-session performance reports from OpenChamber/OpenCode. It added explicit on-demand session usage, useful native compaction result metadata, transactional Git-worktree launch, immutable branch/base/root identity in runtime hydration, and bounded on-demand changed-file/diff review without passive Git polling.

A fourteenth research/audit pass on 2026-08-27 focused on crash recovery and worktree/cwd failures across Pi and lightweight coding harnesses. Worktree creation now durably journals the intended ProjectId/repository/base/branch/path before Git mutation, upgrades the journal after verified creation, conservatively reconciles branch-only/path-only/conflicting states after restart, preserves legitimate descendant commits and dirty task work, and never deletes Git resources automatically. The same pass exposed Pi's native automatic-compaction setting in the GUI with `get_state` reconciliation rather than inventing a local context policy.

A fifteenth implementation pass on 2026-08-27 closed two visible recovery/navigation gaps. Pi's native `get_tree` RPC is now projected on demand through the existing session entry/byte ceilings into a compact full-branch inspector with active-leaf markers and Pi-native fork actions. The renderer now has a bounded crash-loop breaker: two automatic renderer reload attempts are allowed before an explicit recovery screen takes over, while backend-owned Pi runs remain supervised independently of renderer lifetime.

A sixteenth implementation pass on 2026-08-28 closed several Phase 3/4 safety gaps. Live-run admission is now owned by the runtime manager with a conservative configured hard maximum and a runtime-adjustable ceiling that blocks new starts without terminating existing runs. Worktree recovery now offers explicit cleanup only after a fresh exact identity probe, no live run uses the worktree, the checkout is clean, and `HEAD` still equals the captured creation base; branch deletion uses an expected-old ref update and the journal is retired only after a fresh absence proof. Git review now classifies binary changes before requesting a text patch, and renderer request sequencing prevents an older diff result from reappearing after a newer summary refresh. Initial backend hydration can also be retried without forcing a renderer reload.

A seventeenth implementation pass on 2026-08-28 made two previously runtime-only/review-only guarantees durable and executable. The live-run ceiling now persists in an isolated schema-versioned `preferences.json` domain using atomic replacement; malformed, oversized, unsupported, or out-of-range preference state is quarantined independently and safe startup falls back to the configured ceiling without hiding project/worktree state. Git file review now streams bounded byte pages rather than materializing one full patch: continuation cursors carry a path, byte offset, and SHA-256 prefix proof, stale cursors fail closed when earlier patch bytes change, and a separate scan ceiling prevents later pages from repeatedly rescanning an unbounded prefix. Renderer crash-loop policy is factored into a deterministic TypeScript module with repository-owned tests in the quick verification lane.

An eighteenth implementation/hardening pass on 2026-08-28 closed the remaining repository-owned roadmap work. Git review now exposes bounded semantic hunk navigation and backend-owned cancellation/supersession for active review jobs; UTF-8 page boundaries cannot split a scalar. The renderer received keyboard/focus/reduced-motion/accessibility contracts. Windows process-tree termination behavior is regression-tested, terminal-run hot state is bounded, unsupported future draft schemas quarantine rather than downgrade, and the `full` lane now exercises a 25+ MiB session, a multi-megabyte diff, eight concurrent streaming runs, 1,200 historical session files, and cold/warm desktop state startup. Tauri configuration produces an optimized Windows executable and local installer bundles.

A nineteenth usability/safety pass on 2026-08-28 tightened everyday multi-run operation without adding a second runtime authority. Live runs now have exclusive ownership of their canonical execution root, including local checkouts; the launcher and session catalog route users to an existing owner instead of offering a start that the backend will reject. Idle runs have an explicit Close transaction that flushes the current session draft before terminating only the owned Pi process, terminal runs can be dismissed from the hot runtime projection without deleting Pi session JSONL or session-scoped drafts, retained history keeps a four-page renderer window with scroll anchoring, and worktree recovery rows open the already-live owner. Run lists prioritize Needs Attention/working/live/newer runs ahead of retained terminal rows. The desktop can open a run's backend-derived execution folder without accepting an arbitrary renderer path, and failed/quarantined runs now display the backend-bounded failure kind/detail, known process exit code, and termination-uncertainty state. The in-memory session-draft cache now has an explicit count ceiling (256 by default, 4096 hard maximum): only unowned Saved records may be evicted, Dirty/Saving/Failed records are never discarded for capacity, stale asynchronous loads cannot recreate evicted entries, and revisiting an evicted persisted session reloads its exact draft from disk. The optimized Windows executable containing this full hardening pass builds successfully at `target/release/pi-wizard-desktop.exe`.

A twentieth usability/completeness pass on 2026-08-28 closed the remaining first-product launcher and session-discovery gaps. New-run model and thinking choices are now discovered through a bounded ephemeral Pi RPC probe and are applied to the actual child launch before an optional initial task can be submitted; changing the selected model reprobes its supported thinking levels. Context-file loading is exposed as an independent per-launch policy rather than being conflated with project-resource trust, and the same policy reaches local, worktree, recovered-worktree, and resumed-session launches. Project-session discovery now uses bounded, scope-bound continuation cursors so users can explicitly walk older catalog pages instead of permanently losing sessions outside the first window. Cursors bind the canonical project, resolved session directory, normalized query, candidate snapshot, exact path, and modification time; stale or cross-query cursors fail closed and the renderer offers a restart-from-newest action. The 1,200-session scale fixture traverses all pages while preserving per-page scan/entry ceilings. A live smoke against installed Pi 0.84.3 also confirmed `--no-session --no-context-files`, explicit provider/model/thinking launch arguments, `get_state`, model discovery, and model-specific thinking discovery over the real RPC transport.

A twenty-first orchestration/UX pass on 2026-08-28 moved two already-safe backend capabilities into the durable application shell without introducing passive work. Project-session discovery is now a reusable bounded browser shared by New Run and a first-class Recent Sessions view; changing projects invalidates in-flight catalog results through renderer request sequencing, and the view performs no catalog scan until the user explicitly searches. Backend-owned extension dialogs now also appear as actionable cards in a global Needs Attention view using their original run/request IDs, with finite timeout windows ordered ahead of non-expiring requests. The selected run, sidebar, attention queue, and dashboard use the registered `ProjectId` mapping instead of inferring project identity from a worktree path. Run headers expose project, literal local/worktree execution identity, model, thinking level, branch, queue count, and state; dashboard cards project existing bounded compaction/queue/tool/shell state into a high-level activity label, distinguish queued/compacting state, and keep Stop reachable during compaction. Main navigation and selected-run navigation expose explicit `aria-current` state while the existing visible focus/reduced-motion contracts remain intact.

A twenty-second orchestration-completeness pass on 2026-08-28 closed the remaining explicit dashboard-card fields from `DESIGN.md`. Each backend run record now owns its wall-clock start timestamp and freezes a terminal timestamp on exit/failure/quarantine; the versioned hydration contract exposes those values so the renderer can show elapsed lifetime without inferring time from opaque IDs. A single renderer minute tick exists only while at least one live run is retained and performs no backend/process/repository polling; terminal durations stay frozen. When the user explicitly computes a bounded Git change summary in a run, the shell retains only cheap review metadata and shows that last-known review count on the dashboard. This reuses already-computed review knowledge and never turns passive dashboard rows into Git subprocess triggers.

A twenty-third semantic-presentation/recovery pass on 2026-08-28 aligned timeline defaults with the product design without increasing hot-state ownership. Persisted successful tool/shell results now use native collapsed disclosure rows by default while failures stay expanded; bounded output/truncation remains available on explicit expansion. Completed verbose thinking blocks likewise collapse by default, while active streaming and short thinking remain visible. These disclosures use mounted DOM state rather than an unbounded renderer expansion registry. The renderer also enforces the exact hydration schema before applying a backend snapshot, so a future incompatible host/renderer combination becomes an explicit recovery error instead of silently projecting the wrong structure.

A twenty-fourth shell-structure pass on 2026-08-28 made the session view honor the one-inspector-at-a-time architecture. Run details, Changes, and Session Tree now share one compact inspector switcher, start closed, and are conditionally mounted so only the selected inspector contributes DOM/reactive state. Opening Changes performs its first bounded Git summary request on demand and leaving an in-flight Changes inspector cancels its backend-owned review job. Session Tree is mounted only while selected and loads its bounded Pi tree on mount; unmount invalidates any late result. The main bounded history/live timeline and composer remain independent of inspector selection.

A twenty-fifth review-invalidation pass on 2026-08-28 made last-known Git metadata truthful across later agent activity. `RunRecord` now owns a monotonic `changeRevision`, advanced conservatively at every completed Pi tool and direct RPC Bash boundary because extension-defined tool names cannot be safely classified as read-only. The then-current hydration schema v8 carried that revision; the current schema is v9. A previously loaded Changes summary can remain as cheap known metadata, but it becomes explicitly stale when its captured revision differs; any file detail is discarded and an in-flight review is cancelled. Reopening/refreshing Changes performs the next Git observation on demand. Dashboard rows never poll Git and visibly mark an older count as stale instead of presenting it as current.

A twenty-sixth trust/attention clarity pass on 2026-08-28 added a metadata-only launch preflight for exactly the protected project-resource locations documented by Pi: project `.pi` settings/resource/system-prompt paths plus current/ancestor `.agents/skills`. It never reads those resources, never reads or writes `trust.json`, and remains advisory because Pi owns saved/parent/default trust resolution. The launcher explains the selected one-run/inherit policy against that snapshot while continuing to treat context files independently. Extension request cards now also expose whether Pi supplied a timeout and, for finite requests, the approximate remaining window from the last authoritative hydration; no countdown polling loop was added.

A twenty-seventh observability/index-policy pass on 2026-08-28 closed the remaining architecture hardening gap without introducing background telemetry or another session authority. The runtime manager now exposes an explicit bounded diagnostic snapshot containing exact process ownership, retained runtime-state bytes, pending RPC/command/dialog counts, active assistant/tool/direct-Bash projections, UI-backlog bytes/frames, cumulative coalesced/dropped/delivered frames, rehydration pressure, and a fixed one-second recent RPC event/byte window. The desktop adds active Git-review and explicit session-catalog job counts. The renderer samples mounted timeline rows only when the user presses **Refresh diagnostics** and development builds observe long tasks through `PerformanceObserver`; there is no diagnostic polling loop, log file, or passive subprocess. Session-catalog indexing was re-measured rather than assumed necessary: the full 1,200-file fixture, including creation and traversal of every bounded continuation page, completes in about 1.40 seconds on the current Windows development environment. Exact newest-by-mtime ordering plus stale-cursor detection requires lightweight metadata enumeration when no index exists, so the stateless implementation is retained; adding SQLite or a filesystem watcher now would add an owner without demonstrated benefit and could weaken CLI-side change detection if implemented as a naive cache.

A twenty-eighth final security/performance-contract pass on 2026-08-28 removed the packaged renderer's last inline-style dependency. Session Tree now maps its already-capped depth to bounded static CSS classes, allowing production Tauri CSP to use self-only script/style sources with no `'unsafe-inline'` and no script eval. Vite's loopback WebSocket and inline development-style injection live only in `devCsp`; production also denies objects, external base URLs, form submission, and framing. Release checks enforce that split. The deterministic quick lane now also proves a steady Ready runtime emits no periodic dirty wakeup or runtime-revision churn during a quiet window, and repository-owned tests exercise the optional live-Pi smoke harness's bounded streaming capture, rejection handling, output-flood termination, and deadline termination without requiring provider credentials.

A twenty-ninth current-Pi/lightweight-harness reliability audit on 2026-08-28 closed the remaining evidence-backed first-product gaps without expanding into an IDE or a second agent runtime. Write-capable historical Resume now refuses an unterminated Pi JSONL tail instead of risking the next Pi append concatenating records, while read-only history remains tolerant and the authoritative file is never repaired by the app. Launch policy now exposes Pi's one-run extension-discovery disable path independently from project trust/context files, including extension-free launch-option probing and a startup-failure recovery hint. The runtime projects bounded provider retry, summarization retry, compaction reason/abort/failure/overflow-retry, and extension-error state under hydration schema v9. Stop uses native `abort_retry` during provider backoff, normal `abort` after a retry attempt restarts, and exact-process escalation where Pi exposes no matching cancel primitive. A Ready/Working run with unexplained Pi event silence gains a one-shot two-minute advisory through the existing deadline scheduler; it never probes, replays, retries, or relabels the run automatically, and `get_state` can arm the advisory after a missed `agent_start`. Pi's native `set_auto_retry` is exposed as an explicit command without a fabricated recovered enabled-state because current `get_state` omits that flag. Session-catalog preview/search strips only Pi's explicit persisted `<skill ...>...</skill>` generated wrapper so skill instructions do not obscure the user's task, while generic template expansion is left untouched. The bounded slash-command palette now supports Arrow Up/Down/Enter navigation and keeps its selected row visible without adding a command-history store. The final Stop compatibility audit also found that current stable Pi 0.84.3 internally implements exhaustive queue clearing but does not expose the public-docs `clear_queue` RPC command. Pi Wizard now detects this in the existing ephemeral launch-options probe, retains the latest user-visible `queue_update` strings only in a private bounded controller shadow, and uses that shadow solely on explicit `clear_queue` rejection before terminating the exact Pi process. This preserves user queue text while ensuring both normal queues and opaque extension custom continuations cannot run; successful `clear_queue` on newer Pi remains authoritative and reusable. A live no-prompt smoke against installed Pi 0.84.3 confirms extension-free/offline/no-session launch, state/model/thinking discovery, native auto-retry control, and the expected `clear_queue` unavailability over the real RPC transport without making a provider model request.

A thirtieth verification-closure pass on 2026-08-28 made routine core-test concurrency explicit after Windows lifecycle fixtures demonstrated host-CPU-dependent interference under Cargo's unconstrained default test parallelism. The complete routine core suite passes with four test threads, limiting incidental concurrent OS-process fixtures independently from the application's live-run ceiling while still exercising process-tree termination concurrently. The repository `quick`/`standard` lanes own that four-thread harness cap instead of weakening production startup/termination deadlines or retrying failures until green; the expensive ignored fixtures remain serialized separately in `full`.

A thirty-first workflow-orchestration pass on 2026-08-28 adds the requested lightweight automation layer without creating a second agent runtime. The existing RuntimeManager ceiling remains the sole live-session admission authority and its full scale fixture now exercises all eight configured Pi sessions concurrently. A separate bounded atomic automation catalog stores only reusable chain names and ordered prompt text, with independent corruption quarantine. The first-class Automation view can reorder prompts, choose a registered project, select worker concurrency, require Git-isolated worktrees for parallel workers, and optionally reserve one live slot for an LLM supervisor. Chain execution is event-driven from a new semantic run-state channel that is intentionally separate from token/tool renderer traffic; capacity changes wake the same scheduler, each step gets a new Pi session, completed workers close normally to free slots while preserving Pi history/worktrees, and Cancel prevents later launches/directives without killing already-running user workers. Launch/capacity mutation shares one serialization gate so a worktree cannot be created on a stale admission decision, and cancellation is rechecked before not-yet-started Pi processes spawn. The renderer loads automation definitions only while that view is open; execution-only changes use a smaller IPC projection, identical state updates emit no event, and execution rows retain only a UTF-8-safe 1 KiB prompt preview. The optional supervisor is itself a normal Pi run with context/extensions disabled; it receives only bounded worker task/status/last-assistant-result summaries and can issue strict JSON Send/Steer/Follow-up directives to exact active RunIds. Supervision has a 32-cycle default execution ceiling and a 15-minute deadline per model turn, duplicate/unknown/stale directives fail closed, and invalid autonomous output disables supervision rather than compromising the deterministic finite chain. Final concurrency hardening uses the full compact execution UUID in generated automation worktree identities instead of a timestamp-heavy UUIDv7 prefix, refuses to start a supervisor when it would consume the only slot capable of launching the first queued worker, and makes an already-running supervisor yield when later manual-run pressure would otherwise strand queued worker work. Worker completion accepts either a new assistant-message count or an observed real Pi activity transition so a user Stop/abort cannot leave a finite chain waiting forever for text that will never arrive. Scheduler-level failure preserves already-running user workers for inspection but attempts to terminate the app-internal supervisor and marks never-started steps failed. End-to-end fake-Pi desktop integration now exercises the actual scheduler rather than only wire/policy helpers: a three-step local chain launches three distinct child sessions, isolates one rejected middle prompt, continues through the final step, and releases capacity; this caught and fixed an eager `then_some` evaluation that had accidentally required a Git base even when worktrees were disabled. A real-Git parallel fixture drives four automated prompts through two simultaneous live Pi workers and four unique worktrees; this caught and fixed the missing generated sibling worktree-parent creation and now discards a journal intent when creation proves no Git mutation occurred. A supervised real-Git fixture launches both worker and supervisor Pi children, accepts one strict JSON `send` directive, revalidates and routes it back into the exact existing worker, observes the worker's second prompt, and confirms both live slots are released at completion.

## Accepted baseline

- Desktop shell, not an IDE and not a new agent runtime.
- Tauri 2 + Rust/Tokio host.
- SolidJS + TypeScript static renderer.
- Upstream Pi CLI in `--mode rpc` as the main integration boundary.
- One independently owned Pi process per live runtime session.
- Pi session JSONL is the authoritative conversation store.
- Derived indexes/caches are disposable and rebuildable.
- Multi-session orchestration, finite prompt-chain automation, optional Pi-based supervision, Pi-native steer/follow-up controls, Git worktree isolation, and bounded change review are core product scope.
- Transcript virtualization, bounded tool output, lazy diff loading, backpressure, and zero passive polling are baseline architecture requirements.
- Project trust, Git isolation, and sandboxing are distinct concepts in the UI and runtime.

## Implemented foundation

- Rust 2024 workspace with a Tauri-independent `pi-wizard-core` crate and thin `src-tauri` composition root.
- Static SolidJS + TypeScript + Vite renderer scaffold with no production application server.
- Centralized validated runtime limits for RPC frames, outbound payloads, streaming text/content blocks, tool previews, UI backlog, active tools, pending RPC/UI requests, diagnostics, failure detail, image attachments, draft size, recovered Stop queues, project/worktree-registry state, Git command/review payloads, retained runtime state, startup RPC readiness, draft flush/debounce, and Stop deadlines.
- UUIDv7-backed opaque project/run/worktree/draft-image identities and string-compatible RPC request identities.
- Strict incremental LF-only JSONL framing with optional CR handling, Unicode-line-separator safety, oversized-frame recovery, and bounded buffering.
- Typed outbound Pi RPC commands and extension UI responses with exact request correlation and outbound byte ceilings, including first-class `steer`, `follow_up`, current capability/session operations, and stable `get_entries(since)` history cursors.
- Bounded validated Pi image payloads with count, per-image decoded-byte, aggregate decoded-byte, MIME/base64, and final outbound RPC ceilings.
- Forward-compatible inbound response/event classification that preserves unknown valid event payloads, plus typed nested assistant-stream parsing by `contentIndex` and direct Bash update correlation by request ID.
- Typed current tool start/update/end, queue-count, session-name, thinking-level, `get_state`, `clear_queue`, and direct-Bash response projections; accumulated tool output is never mistaken for a delta.
- Async bounded RPC reader/writer primitives plus a supervised child-process transport with private stdin/stdout RPC, bounded continuously drained stderr, exact spawn identity, deadline-bounded process-tree termination, and bounded diagnostic EOF finalization. Windows `.cmd` wrappers terminate through the exact captured PID tree; Unix children launch in an app-owned process group.
- Explicit Pi launch specification with canonical working directory, optional canonical session directory, startup model/thinking choices, Pi saved/default trust inheritance or one-run override, independent context-file policy, current direct-Bash `excludeFromContext`, and other typed Pi launch/runtime controls.
- Launch-environment resolver over explicit configuration, desktop-process environment, and a bounded shell-probe result, with one selected environment shared by Pi/Git discovery and spawning. Diagnostics expose executable/provenance metadata without serializing environment values or secrets.
- Active bounded platform shell-environment probing and bounded `pi --version` probing under the exact resolved launch environment. The Tauri host caches environment plus parsed Pi version as one launch profile so diagnostics and actual process startup cannot diverge.
- Canonical project binding primitive with stable `ProjectId`, exact-path verification, missing-path detection, and explicit-only relocation; no silent project fallback/rebinding. A schema-versioned atomic project registry now preserves those IDs across app restarts, quarantines corrupt/oversized/duplicate derived state, and commits registration/relocation to disk before mutating in-memory indexes.
- Separate process lifecycle and agent activity state, enforced through a reducer rather than public field mutation.
- Explicit terminal `Quarantined` state for runs whose OS-process termination cannot be confirmed; quarantined runs cannot accept further activity mutations.
- Authoritative bounded `get_state` reconciliation for live activity, compaction, session identity/name, model, thinking level, native automatic-compaction state, queue modes, and counts so backend recovery does not depend on renderer memory. Session IDs and the complete retained runtime-state projection have explicit byte ceilings, and incremental session-name events cannot bypass the same retained-state budget.
- Session-scoped generation-safe draft state plus a backend session draft owner and bounded atomic persistence worker. The in-memory owner has an explicit record-count ceiling; only unowned Saved session records are evictable, while Dirty/Saving/Failed records fail closed at capacity. Evicted persisted sessions clear their load-attempt bookkeeping so revisiting them reloads from disk, and stale asynchronous load completion cannot recreate an evicted unowned record. Extension `set_editor_text` uses the same owner; first-known Pi session identity adopts a pending-run draft, later session replacement never carries text into another session, restart restores saved text, corruption is quarantined, and Dirty/Saving/Saved/Failed plus bounded persistence detail remain visible. Shutdown bypasses normal debounce and waits only through the configured flush deadline.
- Pending extension interaction ownership by exact request ID and bounded pending RPC request correlation.
- Per-run RPC controller that atomically owns command barriers, pending correlation, writer-failure rollback, live stream projection, direct-Bash ownership, response completion, and runtime-state event application.
- Persistent `RuntimeManager` that owns all live run records/controllers/process handles, request waiters, extension-dialog response ownership, Stop transactions, renderer backlogs, process failures, and global shutdown. It uses separate normal/control queues and requires an explicit active Tokio runtime rather than panicking on an implicit runtime assumption.
- Explicit pull-based runtime diagnostics project bounded counters from those existing owners rather than starting telemetry work: exact process ownership, retained state bytes, pending RPC/command/dialog counts, live projection counts, UI backlog/coalescing/drop/delivery state, rehydration pressure, recent fixed-window RPC throughput, active Git/session-catalog jobs, mounted timeline rows, and development-only long-task observation.
- Backend-owned live-run admission has a validated configured maximum and a runtime-adjustable ceiling. New starts are rejected before child spawn when the active count reaches that ceiling; lowering the ceiling never kills existing runs, and the renderer cannot bypass the manager owner. The selected ceiling is stored in its own schema-versioned atomic preferences domain. Persistence commits before the manager is changed, failed writes leave the active ceiling untouched, corrupt/out-of-range saved values are quarantined to a safe configured default, and the renderer surfaces the recovery notice.
- Client-side session-replacement and manual-compaction barriers that close Pi RPC's asynchronous-dispatch race window before corresponding semantic events arrive. Persistent session replacement first forces the old session's current dirty draft through the bounded durability boundary; later commands/composer edits cannot overtake that transaction, and failed/expired flush leaves Pi on the old session.
- Accepted `new_session`/`switch_session`/`fork`/`clone` replacement responses automatically queue authoritative `get_state` reconciliation before the original waiter is released, because the replacement response itself does not establish the new Pi session identity. Extension-cancelled replacements leave the existing binding untouched.
- User-facing Stop transaction that bounds and clears/preserves queues before abort, handles settle-during-clear races, keeps the Pi process reusable when native queue clearing and abort settle successfully, and otherwise escalates to exact-process termination without pretending the child remains reusable. Current stable Pi 0.84.3's missing RPC `clear_queue` is handled by the private bounded last-`queue_update` fallback only on explicit rejection, followed by mandatory exact-process termination. Recovered steering/follow-up text is prepended to existing unsent draft text through the same backend draft owner; an oversized merge is reported without destroying the existing draft.
- Semantic RPC response outcomes distinguish protocol rejection, acceptance, and extension-cancelled `new_session`/`switch_session`/`fork`/`clone` operations.
- Bounded backend projection for extension `setStatus`/`setWidget`/`setTitle` state with entry and byte ceilings; transient notifications and draft/editor state remain separate owners.
- Bounded live assistant/tool projections, including ordered text/thinking/tool-call content blocks, sparse-index-safe block ceilings, an aggregate assistant byte ceiling, authoritative block-end replacement, and accumulated tool-output replacement semantics.
- Bounded request-correlated direct-Bash live previews independent from tool-call previews.
- Explicit classification of high-frequency display updates that may be coalesced and must not directly trigger durable app-owned persistence.
- Byte-bounded per-run renderer backlog with keyed replacement for assistant/tool/Bash display frames, accounting for key/frame overhead, display-frame eviction under pressure, and explicit rehydration failure when semantic-only state exceeds capacity.
- Coalesced backend dirty-run signals plus bounded pull-based renderer drains. Ordinary hydration is non-destructive and re-announces any already-pending delivery to a newly subscribed renderer; it never clears another run's transient notification/tool-finished frames. Backlog desynchronization uses an explicit per-run recovery transaction that discards only that run's stale queued frames. Signal loss/lag widens to authoritative hydration rather than raw-event replay or interval polling; IPC enum variants and fields have regression-tested camelCase wire tags.
- Schema-versioned, revisioned hydration schema v9 of runtime semantic state, immutable Git-worktree identity, backend-owned lifecycle timestamps/change invalidation, current durable draft/restore state, backend-derived composer availability/submission state, model image-input capability, capabilities, live assistant/tool/Bash previews, extension UI state/dialogs, live session-sync cursors, bounded compaction/provider-retry/summarization-retry/extension-error state, and the non-authoritative one-shot quiet-stream advisory. Repeated hydration does not restart a live Pi child, and unsupported renderer/host schema combinations fail explicitly.
- Bounded live-history synchronization through `get_entries(since)` with separate append cursor/leaf identity, one request in flight, stale-cursor rejection, and explicit resync-required state rather than fallback to full `get_messages` hydration.
- File-backed bounded active-branch history pages for cold/resumed sessions. Latest/Older/Newer navigation follows persisted `parentId` ancestry, limits line/scan/page/text bytes independently, keeps abandoned branches out of the normal timeline, and binds cursors to the exact Pi session identity. Persisted user entries expose Pi-native fork actions without materializing a whole transcript.
- Bounded on-demand full session-tree inspection uses Pi's native `get_tree` RPC rather than reconstructing a second tree from JSONL. The backend flattens only renderer-safe identity/label/preview metadata, caps node count and retained bytes, preserves active-leaf identity, and exposes Pi-native fork actions at user-message nodes.
- Tauri owns the runtime manager plus project/worktree registries and the isolated preferences owner. Implemented commands cover hydration/capacity control, explicit per-run UI recovery, bounded event draining, local/worktree/recovered-worktree run startup, durable worktree inspection/reconciliation/conservative cleanup, on-demand paged Git review, Pi-native session controls/usage/compaction/tree inspection, draft editing/submission, Stop, idle-run Close, terminal-run Dismiss, backend-derived execution-folder opening, extension dialog responses, and Pi environment/version probing. Production composition uses the bundle-scoped app-data `runtime-state` root for independent draft/project/worktree/preferences persistence domains. Tauri forwards only normalized dirty/rehydrate wakeups, not raw Pi frames.
- Explicit worktree cleanup is serialized against desktop run starts and is intentionally narrow: only a freshly re-proven exact app-created worktree with no live run, no dirty/untracked files, and no commits beyond the captured base is removed. `git worktree remove` is followed by expected-old branch deletion; dirty, advanced, conflicting, or partial states remain intact and recoverable.
- Bounded current-project session discovery/resume now follows Pi's resolved launch environment, `PI_CODING_AGENT_DIR` / `PI_CODING_AGENT_SESSION_DIR`, merged global/project `sessionDir` settings, and default encoded-cwd storage. An explicit page request performs a lightweight directory metadata pass to preserve exact global mtime ordering and bind stale-cursor snapshots, while retained candidates, detailed header/preview scans, page entries, and page bytes remain bounded. Discovery filters session headers by canonical cwd even for flat custom directories, normalizes only Pi's explicit persisted skill wrapper in preview/search, and revalidates a selected JSONL file before launching `--session`; a write-capable Resume additionally requires an LF-terminated final record and never repairs Pi JSONL. No startup/passive catalog scan or persistent index is currently justified by measurements.
- Tauri `ExitRequested` is intercepted until the runtime manager completes bounded shutdown of all children owned when shutdown began. Renderer navigation/reload has no process-lifecycle side effect.
- Canonical project paths reuse a durable app-owned `ProjectId` across desktop restarts. Missing stored paths remain detached registrations; corrupt registry state is quarantined rather than rebound to another checkout.
- Solid renderer installs runtime listeners before initial hydration, folds concurrent per-run dirty signals, drains bounded pages without passive polling, carries hydration demand across multi-batch continuation drains, uses request sequencing so older hydration responses cannot overwrite newer state, and widens backend lag/desynchronization to explicit per-run recovery/fresh hydration.
- Renderer render failures are contained by a top-level Solid error boundary. A session-scoped breaker permits at most two automatic renderer reload attempts before presenting an explicit recovery screen with manual retry/reload controls; a stable ten-second renderer window clears the breaker. Storage failure disables automatic reload rather than risking a loop, error detail is bounded, and the pure breaker policy is covered by a repository-owned TypeScript test invoked by the quick verification lane. Renderer reload remains independent from backend Pi-process lifetime.
- Solid now exposes an explicit existing-project launch form with Pi inherit/approve/ignore resource-trust choices plus independent context-file/extension-discovery policies, local vs inspected-worktree launch, durable worktree recovery inspection/restart and conservative unused-worktree cleanup, runtime-scoped live-run admission control, bounded session search/resume, history paging/fork, session naming/clone, discovered model/thinking/slash-command controls with bounded keyboard palette navigation, Pi-native automatic/manual compaction, explicit native provider auto-retry control, on-demand usage, bounded recovery notices for retry/compaction/extension/stall state, bounded on-demand Git change review, a backend-owned Send/Steer/Follow-up composer, Stop across work/compaction/retry states, visible draft durability/restore errors, and bounded live assistant/thinking/tool/Bash output including truncation markers. Failed draft IPC synchronization is retained locally for explicit retry instead of being overwritten by a later hydration.
- Git review returns explicit binary-file metadata instead of attempting to render binary patches. Text diffs stream one bounded byte window at a time; continuation cursors are bound to the project-relative path plus a SHA-256 digest of every preceding raw diff byte, so a changed patch fails as stale rather than mixing repository observations. Each page has independent retained-byte and prefix-scan ceilings, and the renderer keeps only the current page plus bounded cursor metadata with Previous/Next navigation. Summary refresh invalidates older in-flight file-detail ownership so stale async diff results cannot overwrite a newer repository observation.
- Image drafts support picker, paste, drag/drop, removal, session-scoped persistence/restore, image-only prompts, and generation-safe submission. Raw base64 stays backend-owned and hydration exposes metadata only. Pi's declared model `input` capability is projected as `supportsImages`; explicit text-only models disable new image ingestion and backend submission while preserving existing draft images for removal or a later model switch.
- Renderer `Needs Attention` UI now presents typed Pi extension select/confirm/input/editor dialogs across runs, responds by exact request ID through the priority manager control plane, preserves partially typed local input across same-ID hydration refreshes, and refreshes authoritative state after stale/expired-response rejection.
- Process `Starting` now means the OS child exists but Pi RPC readiness has not yet been proven. A designated startup `get_state` must succeed before `Ready`; its deadline is part of the manager's normal deadline selector, a silent child is terminated/failed, and explicit Stop/application shutdown supersede the startup timer.
- Static renderer Content Security Policy instead of a null CSP.
- Local `quick`, `standard`, and `full` verification commands, with the full lane owning deterministic release/scale/startup checks and an optimized desktop build.
- Deterministic fake-Pi subprocess coverage in addition to in-memory protocol/state fixtures.

## Known upstream limitation

- Direct in-place navigation to an arbitrary historical branch is not exposed by the current Pi RPC command surface. The full branch tree is visible and user-message nodes invoke Pi-native fork semantics; Pi Wizard does not invent a renderer-only branch mutation.

## Deliberately deferred

- embedded terminal emulator;
- full source editor or file explorer;
- multi-provider harness abstraction beyond Pi;
- remote/mobile clients;
- daemon/background service surviving desktop exit;
- scheduled autonomous jobs;
- custom plugin marketplace;
- application-owned model/provider authentication;
- fake GUI permission modes that Pi itself does not enforce;
- built-in container/VM sandbox implementation.
- branch integration/commit/conflict-resolution workflows; the first review surface is intentionally review-only.

These are not current gaps. Do not work on or report them as remaining work unless the user explicitly asks for them.
